# Build-Validation Exhibits Index

Physical/GUI exhibits captured to supplement the terminal-only Build 0a validation
evidence already in `logs/transcripts/`. Each entry is custody-chained via
`custody_append.py` into `chain_of_custody.log` (mode=`exhibit`) immediately after
the file is confirmed present. `session_id` is `build-0a-evidence` for the original
Build 0a/0b batch and `build-0d-evidence` for the Build 0d RRC-vs-N2 spike batch.

| File | Captured (UTC, custody timestamp) | What it shows | Supports |
|---|---|---|---|
| `screenshots/SDR-Setup.jpg` | 2026-07-29T23:37:09Z | Physical RF-enclosure/bench photo (phone camera, Pixel 8 Pro, on-device timestamp 2026-07-30 11:33:02 local) | Chapter 3 testbed-evidence section — physical hardware setup |
| `screenshots/Subscribers.png` | 2026-07-29T23:45:12Z | Open5GS WebUI (http://localhost:9999) Subscriber list — all 8 profiles visible (001010000000001-008: UE1-5 physical + SW-Std/Ext/Min) | Chapter 3 testbed-evidence section — subscriber provisioning |
| `screenshots/ngap_label0.pca_clean-baseline.png` | 2026-07-30T01:59:18Z | Wireshark decode of `ngap_label0_full.pcap` frame 161 (proxy->AMF, port 38413): SW-Std Stage 1 baseline as it reached the AMF — `bandNR:78`, `supportedBandCombinationList` present (CA on), `maxNumberMIMO-LayersPDSCH: twoLayers`. Superseded an earlier pair of screenshots that captured only the pre-rewrite (gNB->proxy, port 38412) side and were visually identical between label 0 and label 5 — those were discarded, not used as evidence. | Chapter 3 testbed-evidence section, Build 0a step 4/5 validation — Stage 1 rewrite visible on N2 |
| `screenshots/ngap_label5.pcap_Combined-attack.png` | 2026-07-30T01:59:18Z | Wireshark decode of `ngap_label5_full.pcap` frame 56 (proxy->AMF, port 38413): label 5 (Combined) attack as it reached the AMF — no `supportedBandCombinationList` (CA stripped), no `maxNumberMIMO-LayersPDSCH` (DL MIMO reduced), `maxNumberMIMO-LayersCB-PUSCH: oneLayer` (UL forced to SISO), no `ims-Parameters` (VoNR denied) | Chapter 3 testbed-evidence section, Build 0a step 5 validation — Stage 2 attack modifier visible on N2 |
| `../../../data/raw/exhibits/ngap_label0_full.pcap` | 2026-07-30T01:59:18Z | Full N2 capture (both proxy legs, ports 38412+38413) for the SW-Std label-0 registration underlying the screenshot above | Raw evidence backing the label-0 screenshot |
| `../../../data/raw/exhibits/ngap_label5_full.pcap` | 2026-07-30T01:59:18Z | Full N2 capture (both proxy legs, ports 38412+38413) for the SW-Std label-5 registration underlying the screenshot above | Raw evidence backing the label-5 screenshot |
| `screenshots/gqrx_carrier_3489.42MHz.png` | 2026-07-30T04:57:57Z | GQRX (HackRF One) live spectrum + waterfall, independent RF confirmation of the srsRAN gNB transmitting: sharp carrier spike at 3489.420000 MHz (~-35 to -40 dBFS against ~-85 to -90 dBFS noise floor), periodic TDD burst pattern visible in the waterfall. Settings: 20 Msps, LNA/IF 16 dB, VGA/BB 20 dB, RF amp off. Supersedes an earlier wideband (3470-3510 MHz) hackrf_sweep attempt that was inconclusive (matches the prior HackRF-sweep caveat already on file in the project's internal build log) — a narrowband gqrx view at correct gain settings resolved it cleanly. | Chapter 3 testbed-evidence section — independent RF (physical-layer) confirmation of on-air transmission, alongside the protocol-level NG Setup/registration evidence |
| `../../../data/raw/exhibits/ngap_label6_full.pcap` | 2026-07-30T06:51:33Z | Full N2 capture (both proxy legs, ports 38412+38413) for a SW-Std label-6 (Partial/noise) registration — `ngap_proxy.py --stage rewrite --profile sw-std --label 6` against UERANSIM `nr-gnb`/`nr-ue` (no live RF; signalling-path capture only). `features/extract_features.py extract()` confirms both legs decode: pre-rewrite (38412) minimal native capability, post-rewrite (38413) the SW-Std Stage 1 baseline (CA on, 2-layer DL/1-layer UL MIMO, VoNR on) with the deterministic `random_state=42` RNG's first draw (~0.639, no flip) — the flipped-bit case for this seed only appears on the *second* registration within one proxy process (see the label-6 validation-tier capture and the project's internal build log's Build 0a session note); this single-registration exhibit documents the no-flip branch. The bit itself (a ROHC profile flag) is outside the 12 tracked features, so this exhibit's evidentiary value is the registration succeeding end-to-end under label 6, not a visible field change. | Chapter 3 testbed-evidence section, Build 0a step 5 validation — label 6 (Partial/noise) end-to-end registration under the proxy |
| `screenshots/ngap_label1.pcap_Cat-downgrade.png` | 2026-07-30T08:54:31Z | Wireshark decode of `data/raw/validation/ngap_swstd_label1_full.pcap` frame 42 (proxy->AMF, port 38413): label 1 (Cat downgrade) attack as it reached the AMF, full tree expanded down to `UE-NR-Capability` — `accessStratumRelease: spare1 (7)`, vs. `rel15 (0)` on the label-0 baseline (same tree path, see `ngap_label0.pca_clean-baseline.png`). All sibling fields (`pdcp-Parameters`, `rf-Parameters`, `featureSets`, `nonCriticalExtension`) unchanged from baseline, showing the attack is isolated to the one field. Captured after the Build 0b fix that re-targeted `apply_cat_downgrade` from an untracked `pdcp-Parameters`/`featureSets` change to `accessStratumRelease` — see the project's internal build log's "Build 0b fix" section for why `spare1` and not the architecture doc's literal "rel8" was used (NR's `AccessStratumRelease` enum has no codepoint below `rel15`). | Chapter 3 testbed-evidence section, Build 0b label-1 fix validation — Stage 2 attack modifier visible on N2 |
| `screenshots/ngap_label2.pcap_CA-disabled.png` | 2026-07-30T10:15:08Z | Wireshark decode of `data/raw/validation/ngap_swstd_label2_full.pcap` frame 40 (proxy->AMF, port 38413): label 2 (CA disabled) attack as it reached the AMF — `rf-Parameters` fully expanded showing only `supportedBandListNR` (single `bandNR: 78` entry); no `supportedBandCombinationList` child at all, the absence being the evidence (contrast against the baseline, where it's present). | Chapter 3 testbed-evidence section, Build 0a/0b step 5 validation — Stage 2 CA-disabled modifier visible on N2 |
| `screenshots/ngap_label3.pcap_MIMO-reduced.png` | 2026-07-30T10:15:08Z | Wireshark decode of `data/raw/validation/ngap_swstd_label3_full.pcap` frame 40 (proxy->AMF, port 38413): label 3 (MIMO reduced) attack as it reached the AMF — both `featureSets` subtrees expanded in one shot: `featureSetsDownlinkPerCC` has no `maxNumberMIMO-LayersPDSCH` field (absent = 1-layer DL, per the DL enum's lack of a `oneLayer` value), and `featureSetsUplinkPerCC -> mimo-CB-PUSCH -> maxNumberMIMO-LayersCB-PUSCH: oneLayer (0)` (UL explicitly forced to SISO). CA (`rf-Parameters`) and VoNR (`nonCriticalExtension -> ims-Parameters`) untouched, isolating the attack to MIMO only. | Chapter 3 testbed-evidence section, Build 0a/0b step 5 validation — Stage 2 MIMO-reduced modifier visible on N2 |
| `screenshots/ngap_label4.pcap_VoNR-denied.png` | 2026-07-30T10:15:08Z | Wireshark decode of `data/raw/validation/ngap_swstd_label4_full.pcap` frame 40 (proxy->AMF, port 38413): label 4 (VoNR denied) attack as it reached the AMF — `nonCriticalExtension -> nonCriticalExtension` expanded showing no `ims-Parameters` child at all (contrast against `ngap_label0.pca_ims-parameters-baseline.png`, where the same nested node contains `ims-Parameters`). `interRAT-Parameters` (a sibling under the outer `nonCriticalExtension`) is untouched. | Chapter 3 testbed-evidence section, Build 0a/0b step 5 validation — Stage 2 VoNR-denied modifier visible on N2 |
| `screenshots/ngap_label0.pca_ims-parameters-baseline.png` | 2026-07-30T10:15:08Z | Wireshark decode of `ngap_label0_full.pcap` frame 161 (proxy->AMF, port 38413): SW-Std label-0 baseline, `nonCriticalExtension -> nonCriticalExtension -> ims-Parameters` present — the "before" half of the label-4 VoNR-denied pair above | Baseline contrast for the label-4 VoNR-denied exhibit |
| `screenshots/ngap_label6.pcap_Partial-noise-flip.png` | 2026-07-30T10:15:08Z | Wireshark decode of `data/raw/validation/ngap_swstd_label6_full.pcap` frame 103 (proxy->AMF, port 38413, the *second* registration in the same proxy process): label 6 (Partial/noise) attack — `pdcp-Parameters -> supportedROHC-Profiles -> profile0x0004: True` highlighted, the seeded `random_state=42` RNG's flip on its second draw (~0.025). The first registration (frame 56, not used here — see `ngap_swstd_label6_full.pcap`) lands on the no-flip branch and is visually identical to baseline, which is why the flip only shows up on this second-registration frame. Contrast against `ngap_label0.pca_rohc-profiles-baseline.png`, where the same field is `False`. | Chapter 3 testbed-evidence section, Build 0a/0b step 5 validation — Stage 2 Partial/noise bit-flip visible on N2 |
| `screenshots/ngap_label0.pca_rohc-profiles-baseline.png` | 2026-07-30T10:15:08Z | Wireshark decode of `ngap_label0_full.pcap` frame 161 (proxy->AMF, port 38413): SW-Std label-0 baseline, `pdcp-Parameters -> supportedROHC-Profiles`, every profile bit `False` — the "before" half of the label-6 partial-noise pair above | Baseline contrast for the label-6 partial-noise exhibit |
| `screenshots/ngap_label1.pcap_frame55_pre-rewrite-rel15.png` | 2026-07-30T11:49:22Z | Wireshark decode of `../../../data/raw/rrc/spike_verify_label1.pcap` frame 55 (gNB->proxy, port 38412, real handset Samsung A56): `UE-NR-Capability -> accessStratumRelease: rel15 (0)` — the native, pre-rewrite value the handset actually sent | Build 0d RRC-vs-N2 spike, real-handset "before" half — real handsets skip Stage 1, so this is also the untampered wire value, not just a Stage 1 baseline |
| `screenshots/ngap_label1.pcap_frame58_post-rewrite-spare1.png` | 2026-07-30T11:49:22Z | Wireshark decode of `../../../data/raw/rrc/spike_verify_label1.pcap` frame 58 (proxy->AMF, port 38413, same registration as above): `UE-NR-Capability -> accessStratumRelease: spare1 (7)` highlighted — the label-1 (Cat downgrade) tampered value that actually reached the AMF | Build 0d RRC-vs-N2 spike, real-handset "after" half — same field, same registration, only the Stage 2 attack modifier applied |
| `screenshots/rrc_capture_label1_ue_category15.png` | 2026-07-30T11:49:22Z | `../../../data/raw/rrc/rrc_1_a56_build-0d-verify3-20260730_0.json` lines 1-14, `"ue_category": 15` highlighted — the derived `record` field (from `rrc_capture.py`), plus the join metadata above it (`label`, `profile`, `session_id`, `ran_ue_ngap_id`, `rrc_timestamp_utc`, `ngap_tx_timestamp_utc`, `imsi-001010000000001`) tying this RRC capture to the same registration as the two Wireshark frames above | Build 0d RRC-vs-N2 spike — the gNodeB-side untampered reference, captured independently of the proxy |
| `screenshots/rrc_capture_label1_accessStratumRelease-rel15.png` | 2026-07-30T11:49:22Z | `../../../data/raw/rrc/rrc_1_a56_build-0d-verify3-20260730_0.json` line 27, `"accessStratumRelease": "rel15"` highlighted inside `raw_rrc_capability` — the raw decoded RRC field `ue_category` above is derived from | Build 0d RRC-vs-N2 spike — same evidence as the previous entry, at the raw-field level rather than the derived-record level |
| `../../../data/raw/rrc/spike_verify_label1.pcap` | 2026-07-30T11:49:28Z | Full N2 capture (both proxy legs, ports 38412+38413) for the label-1 registration underlying the two Wireshark screenshots above | Raw evidence backing the Build 0d frame-55/frame-58 screenshots |
| `../../../data/raw/rrc/rrc_1_a56_build-0d-verify3-20260730_0.json` | 2026-07-30T11:49:28Z | Full RRC capture record (from `rrc_capture.py`) underlying the two RRC JSON screenshots above | Raw evidence backing the Build 0d RRC-side screenshots |
| `screenshots/phone_pixel8_attached_5gsa_20260808.png` | 2026-08-08T08:11:36Z | G-NetTrack Lite on the Pixel 8 Pro (adb screencap): `Operator: srsRAN 5G Test`, `Type: 5G SA`, `Band: N78`, `RSRP: -74` | Chapter 3 testbed-evidence section — real-handset attachment confirmation, baseline data-collection session |
| `screenshots/wireshark_n2_flow_20260808.png` | 2026-08-08T08:21:04Z | Wireshark, `baseline_realme.pcap` filtered on `ngap`: `InitialUEMessage`/`InitialContextSetupRequest`/`InitialContextSetupResponse` N2 flow | Chapter 3 testbed-evidence section — N2 NGAP flow confirmation, baseline data-collection session |
| `screenshots/gnb_console_cellup_20260808.png` | 2026-08-08T08:24:18Z | Live srsRAN gNB terminal (qterminal, ImageMagick `import -window`): real-time PHY/MAC scheduler output (PDSCH/PUCCH/SINR) for an actively connected UE | Chapter 3 testbed-evidence section — gNB console cell-up confirmation, baseline data-collection session |
| `screenshots/phone_a56_attached_5gsa_20260808.png` | 2026-08-08T09:15:17Z | G-NetTrack Lite on the Samsung A56 (adb screencap): shows `Operator: 00101`, `ARFCN: 632256`, `Band: N78`, `RSRP: -73` (our testbed cell, confirmed live against `amf.log`'s concurrent `imsi-001010000000001` registration activity), but also `Type: 5G NSA` / `eNB: 12153` from a real commercial "One NZ" LTE cell (MCC 530/MNC 01). **Caveat, confirmed via `adb shell dumpsys isub` and `dumpsys telephony.registry` on-device**: this A56's logical SIM slot 1 (subId=4) is provisioned as `displayName=srsRAN Test, mcc=001, mnc=01` — our test USIM, correctly the phone's active default data SIM — so this is the right device and the right SIM. The "eNB 12153" reading is the modem's own standards-compliant emergency-fallback behavior: when our testbed's RF link is momentarily unstable (documented SINR instability, the project's internal build log's antenna-positioning note — confirmed still present at capture time, SINR swinging -25dB to +24dB within the same few seconds), the modem searches for and briefly camps on a visible real commercial tower for emergency-only service (`registrationState=DENIED`, `mIsEmergencyOnly=true` in the telephony dump), and G-NetTrack's UI blends that reading with the primary 5G SA one, mislabeling `Type` as NSA. Not a misconfiguration or wrong-device capture — a genuine, diagnosed side effect of the testbed's known RF-instability issue. | Chapter 3 testbed-evidence section — real-handset attachment confirmation, baseline data-collection session; also documents a live-observed RF-instability side effect |
| `screenshots/phone_realme_attached_5gsa_20260808.png` | 2026-08-08T09:23:06Z | G-NetTrack Lite on the Realme RMX3363 (adb screencap, serial f74439fa): `Operator: srsRAN Test`, `Type: 5G SA`, `Band: N78`, `RSRP: -74`, live `UL: 344 kbps / DL: 1357 kbps` data throughput — clean reading, no RF-instability artifact this time. Device only exposed a MIDI USB interface (no ADB) for most of this session; became reachable after an on-device USB-mode change, then required `adb shell input keyevent KEYCODE_WAKEUP` first since the screen was asleep (first capture attempt was a black frame). | Chapter 3 testbed-evidence section — real-handset attachment confirmation, baseline data-collection session |
| `screenshots/wireshark_label0_pixel8_beforeafter_20260809.png` | 2026-08-08T21:07:36Z | Wireshark decode of `data/raw/0/capture_0_pixel8_pixel8-0-run01-20260809T084559_1.pcap` frame 25 (`ngap.procedureCode==44`), full tree expanded down to `UE-NR-Capability` — `accessStratumRelease: rel16 (1)`, all parameter branches (`pdcp`/`rlc`/`mac`/`phy`/`rf-Parameters`, `measAndMobParameters`, `fdd`/`tdd-Add-UE-NR-Capabilities`, `featureSets`, `featureSetCombinations: 2 items`), matching the concurrently-extracted `raw_12f.csv` row (`ue_category=16, ca_band_count=2`). Session B real-handset collection, Pixel 8, label 0 — the first-event exhibit checkpoint; since label 0 is itself the baseline, this is a single-pane capture rather than a before/after pair (labels 1/3/4/5's checkpoints contrast against this frame). | Chapter 3 testbed-evidence section — Session B label-0 (Pixel 8) baseline N2 capability decode |
| `screenshots/wireshark_label1_pixel8_beforeafter_20260809.png` | 2026-08-08T21:51:56Z | Side-by-side Wireshark: left = `capture_0_pixel8_..._1.pcap` frame 25 (label-0 baseline, `accessStratumRelease: rel16 (1)`); right = `capture_1_pixel8_pixel8-1-run01-20260809T093353_1.pcap` frame 58 (`UERadioCapabilityInfoIndication`, the post-rewrite gNB->AMF leg — real handsets receive Stage 2 only), `accessStratumRelease: spare1 (7)`. All sibling fields (ROHC profiles, RLC/MAC/PHY parameters) identical between the two, isolating the attack to the one field — matches the concurrently-extracted `raw_12f.csv` row (`ue_category` 16->1). First attempt mistakenly had frame 54 (a bundled retransmission) selected on the right instead of frame 58, showing no difference; corrected before filing. | Chapter 3 testbed-evidence section — Session B label-1 (Pixel 8, Cat-downgrade) before/after N2 capability decode |
| `screenshots/wireshark_label3_pixel8_beforeafter_20260809.png` | 2026-08-08T23:26:04Z | Side-by-side Wireshark: left = `capture_0_pixel8_..._1.pcap` frame 25 (label-0 baseline) — `featureSetsDownlinkPerCC[0] -> maxNumberMIMO-LayersPDSCH: fourLayers`, `featureSetsUplinkPerCC[0] -> mimo-CB-PUSCH -> maxNumberMIMO-LayersCB-PUSCH: twoLayers`; right = `capture_3_pixel8_pixel8-3-run01-20260809T105054_1.pcap` frame 56 (post-rewrite, port 38413) — DL field absent entirely (no `maxNumberMIMO-LayersPDSCH` child under `featureSetsDownlinkPerCC[0]` at all, the 3GPP absent-means-1-layer convention), UL forced to `oneLayer`. Matches `raw_12f.csv` (`mimo_layers_dl/ul` 4/2 -> 1/1). Two earlier attempts showed no visible change because Wireshark's detail pane hadn't refreshed after re-selecting frame 56 (stale tree from a prior frame); independently confirmed the real diff first via `ngap_decode`/pycrate (a full recursive dict diff of frame 53 vs. 56, isolating exactly these two fields) before asking for a re-take, which then matched. | Chapter 3 testbed-evidence section — Session B label-3 (Pixel 8, MIMO-reduced) before/after N2 capability decode |
| `screenshots/wireshark_label5_pixel8_beforeafter_20260809.png` | 2026-08-09T01:35:56Z | Side-by-side Wireshark: left = `capture_0_pixel8_..._1.pcap` frame 25 (label-0 baseline) — `rf-Parameters` has `supportedBandCombinationList: 2 items` and `supportedBandCombinationList-v1540: 2 items`; right = `capture_5_pixel8_pixel8-5-run01-20260809T130053_1.pcap` frame 81 (post-rewrite, port 38413) — both fields entirely absent (only `supportedBandListNR` remains), CA disabled. Matches `raw_12f.csv` (`ca_supported` True->False, `ca_band_count` 2->0); this device declares CA via the `-v1540` release-extension variant, not the base key alone, both stripped identically. MIMO reduction (also part of the Combined attack) is documented separately by the label-3 exhibit rather than repeated here. Located via a recursive `ngap_decode` dict diff after the base `supportedBandCombinationList` key (checked first, by analogy with the label-2 exhibit) turned out empty for this device/registration - the real CA declaration for this capture lives under the `-v1540` variant. | Chapter 3 testbed-evidence section — Session B label-5 (Pixel 8, Combined) before/after N2 capability decode |
| `screenshots/phone_pixel8_speedtest_label0_20260809.png` | 2026-08-09T02:20:54Z | fast.com (Chrome, adb screencap) on the Pixel 8 under label 0 (proxy `--label 0`, no attack) — `14 Mbps`, `5G` indicator visible in the status bar. | Chapter 3 testbed-evidence section — Session B before/after speedtest pair, label-0 half |
| `screenshots/phone_pixel8_speedtest_label5_20260809.png` | 2026-08-09T02:24:08Z | fast.com (Chrome, adb screencap) on the Pixel 8 under label 5 (Combined attack, proxy switched and the UE re-registered fresh before testing so the gNB scheduler actually picked up the reduced capability) — `19 Mbps` download, `12 Mbps` upload, `43ms`/`636ms` unloaded/loaded latency. **Caveat**: throughput was higher, not lower, under the attack this run. Not treated as evidence the attack has no effect - the NGAP capability fields themselves are unambiguous (see the label-5 Wireshark exhibit above) - but as a reasonable single-cell-testbed limitation: there is only one physical cell/band here, so stripping `CA supported` from the advertised capability costs nothing measurable since carrier aggregation was never actually usable in this topology regardless; real-world RF variance between the two independent test runs plausibly dominates the raw Mbps difference. Documented honestly rather than the pair being presented as a clean before/after throughput drop. | Chapter 3 testbed-evidence section — Session B before/after speedtest pair, label-5 half, with methodological caveat |
| `screenshots/wireshark_label0_realme_beforeafter_20260809.png` | 2026-08-09T03:29:48Z | Wireshark decode of `data/raw/0/capture_0_realme_realme-0-run01-20260809T152334_1.pcap` frame 13 (`ngap.procedureCode==44`), full tree expanded down to `UE-NR-Capability` — `accessStratumRelease: rel15 (0)`, all parameter branches, `featureSetCombinations: 1 item`, matching the concurrently-extracted `raw_12f.csv` row (`ue_category=15, ca_band_count=1, mimo_dl/ul=4/1, vonr_supported=True, nr_band_count=13`) — the documented Realme fingerprint. Session B real-handset collection, Realme RMX3363, label 0 — single-pane baseline capture (label 0 is itself the baseline, same convention as the Pixel 8 label-0 exhibit). | Chapter 3 testbed-evidence section — Session B label-0 (Realme) baseline N2 capability decode |
| `screenshots/wireshark_label1_realme_beforeafter_20260809.png` | 2026-08-09T04:14:18Z | Side-by-side Wireshark: left = `capture_0_realme_..._1.pcap` frame 16 (label-0 baseline, `accessStratumRelease: rel15 (0)`); right = `capture_1_realme_realme-1-run01-20260809T160432_1.pcap` frame 34 (`UERadioCapabilityInfoIndication`, post-rewrite gNB->AMF leg), `accessStratumRelease: spare1 (7)`. All sibling fields identical, isolating the attack to the one field — matches the concurrently-extracted `raw_12f.csv` row (`ue_category` 15->1). Correct frame selected on the first attempt this time. | Chapter 3 testbed-evidence section — Session B label-1 (Realme, Cat-downgrade) before/after N2 capability decode |
| `screenshots/wireshark_label3_realme_beforeafter_20260809.png` | 2026-08-09T05:34:51Z | Side-by-side Wireshark: left = `capture_0_realme_..._1.pcap` frame 16 (label-0 baseline) — `featureSetsDownlinkPerCC[0] -> maxNumberMIMO-LayersPDSCH: fourLayers`; right = `capture_3_realme_realme-3-run01-20260809T172526_1.pcap` frame 27 (post-rewrite, port 38413) — same `Item 0 -> FeatureSetDownlinkPerCC` branch, field entirely absent (only `supportedSubcarrierSpacingDL`/`supportedBandwidthDL`/`channelBW-90mhz`/`supportedModulationOrderDL` remain). Matches `raw_12f.csv` (`mimo_layers_dl` 4->1). First attempt only showed the collapsed `featureSets` item-count level (identical between baseline/attack since the number of CC entries doesn't change, only a field inside one of them); corrected by expanding into the specific `FeatureSetDownlinkPerCC` leaf. | Chapter 3 testbed-evidence section — Session B label-3 (Realme, MIMO-reduced) before/after N2 capability decode |
| `screenshots/wireshark_label4_realme_beforeafter_20260809.png` | 2026-08-09T06:26:37Z | Side-by-side Wireshark: left = `capture_0_realme_..._1.pcap` frame 16 (label-0 baseline) — `nonCriticalExtension -> nonCriticalExtension -> ims-Parameters -> ims-ParametersFRX-Diff -> voiceOverNR: supported (0)`; right = `capture_4_realme_realme-4-run01-20260809T181347_1.pcap` frame 18 (post-rewrite, port 38413) — the nested `nonCriticalExtension` has no expand arrow at all, `ims-Parameters` entirely absent. Matches `raw_12f.csv` (`vonr_supported` True->False). Unlike the Pixel 8 (which never advertised VoNR natively, making label 4 a no-op there), the Realme genuinely supports it, so this is the project's one real before/after VoNR-denied capture. Two earlier attempts on this pair showed no change / the wrong frame's stale content before a correctly-refreshed re-take confirmed the field's true absence, cross-checked first via a direct `ngap_decode` decode of both frames. | Chapter 3 testbed-evidence section — Session B label-4 (Realme, VoNR-denied) before/after N2 capability decode |
| `screenshots/phone_realme_vonr_setting_20260809.png` | 2026-08-09T06:27:08Z | Realme RMX3363 SIM info & settings screen ("srsRAN Test" carrier, adb screencap): `VoLTE calls` toggle present, but no dedicated VoNR/Voice-over-5G toggle anywhere in the list (Data roaming, VoLTE calls, Wi-Fi Calling, Preferred network type, Access point names, Carrier). Different mechanism than the Pixel 8's absence: the Realme's RRC capability genuinely advertises `voiceOverNR: supported` (unlike the Pixel, which never does) - Android's VoNR UI toggle is typically gated behind carrier-config provisioning separately from modem capability, and this private testbed has no official carrier config, so the toggle doesn't surface even though the underlying capability is real. | Chapter 3 testbed-evidence section — Session B label-4 (Realme) VoNR-UI-vs-capability distinction |
| `screenshots/wireshark_label5_realme_beforeafter_20260809.png` | 2026-08-09T07:12:45Z | Side-by-side Wireshark: left = `capture_0_realme_..._1.pcap` frame 16 (label-0 baseline) — `rf-Parameters` has `supportedBandCombinationList: 1 item`, `-v1540: 1 item`, `-v1560: 1 item`; right = `capture_5_realme_realme-5-run01-20260809T190814_1.pcap` frame 27 (post-rewrite, port 38413) — all three entirely absent, only `supportedBandListNR`/`appliedFreqBandListFilter` remain. Matches `raw_12f.csv` (`ca_supported` True->False, `ca_band_count` 1->0); unlike the Pixel, the Realme declares CA via the base key as well as both release-extension variants, all three stripped identically. MIMO/VoNR reduction (also part of the Combined attack, confirmed via direct `ngap_decode` diff) documented separately by the label-3/label-4 exhibits rather than repeated here. Correct frame selected on the first attempt. | Chapter 3 testbed-evidence section — Session B label-5 (Realme, Combined) before/after N2 capability decode |
| `screenshots/phone_realme_speedtest_label0_20260809.png` | 2026-08-09T08:01:25Z | fast.com (Chrome, adb screencap) on the Realme under label 0 (no attack) — `35 Mbps` download, `29 Mbps` upload, `33ms`/`141ms` unloaded/loaded latency. First capture attempt caught G-NetTrack Lite (network monitor, `Data: IDLE`) instead of a real speedtest result - corrected once a genuine fast.com run was confirmed. | Chapter 3 testbed-evidence section — Session B before/after speedtest pair, label-0 half |
| `screenshots/phone_realme_speedtest_label5_20260809.png` | 2026-08-09T08:04:33Z | fast.com (Chrome, adb screencap) on the Realme under label 5 (Combined attack, UE re-registered fresh first) — `40 Mbps` download, `27 Mbps` upload, `21ms`/`119ms` latency. **Same pattern as the Pixel 8 pair**: throughput higher and latency lower under the attack, not worse - reinforces that this is a genuine single-cell-testbed limitation (no real second component carrier to lose, RF/scheduling variance dominates the raw Mbps number) rather than a one-off fluke, now observed consistently across two independent real handsets. | Chapter 3 testbed-evidence section — Session B before/after speedtest pair, label-5 half, cross-device confirmation of the methodological caveat |
| `screenshots/wireshark_label0_a56_beforeafter_20260809.png` | 2026-08-09T10:00:12Z | Wireshark decode of `data/raw/0/capture_0_a56_a56-0-run01-20260809T215627_1.pcap` frame 32 (`ngap.procedureCode==44`), full tree expanded down to `UE-NR-Capability` — `accessStratumRelease: rel15 (0)`, all parameter branches, `featureSetCombinations: 2 items`, matching the concurrently-extracted `raw_12f.csv` row (`ue_category=15, ca_band_count=2, mimo_dl/ul=4/1, vonr_supported=True, nr_band_count=12`), the documented A56 fingerprint (imsi ...0001). Session B real-handset collection, Samsung A56, label 0 — single-pane baseline capture, same convention as the Pixel 8/Realme label-0 exhibits. | Chapter 3 testbed-evidence section — Session B label-0 (A56) baseline N2 capability decode |
| `screenshots/wireshark_label0_nothing3a_beforeafter_20260810.png` | 2026-08-10T08:09:14Z | Wireshark decode of `data/raw/0/capture_0_nothing3a_nothing3a-0-run01-20260810T175015_1.pcap` frame 42 (`ngap.procedureCode==44`), full tree expanded down to `UE-NR-Capability` — `accessStratumRelease: rel16 (1)`, `featureSetCombinations: 1 item`, matching the concurrently-extracted `raw_12f.csv` row (`ue_category=16, ca_band_count=1, mimo_dl/ul=4/1, vonr_supported=True, nr_band_count=10`) — the Nothing 3A's fingerprint (imsi ...0003). Session B real-handset collection, Nothing 3A (Nothing Phone 3a, model A059), label 0 — single-pane baseline capture, same convention as the other real-handset label-0 exhibits. | Chapter 3 testbed-evidence section — Session B label-0 (Nothing 3A) baseline N2 capability decode |
| `screenshots/wireshark_label1_nothing3a_beforeafter_20260810.png` | 2026-08-10T08:20:36Z | Side-by-side Wireshark: left = `capture_0_nothing3a_..._1.pcap` frame 42 (label-0 baseline, `accessStratumRelease: rel16 (1)`); right = `capture_1_nothing3a_nothing3a-1-run01-20260810T175015_1.pcap` frame 34 (`UERadioCapabilityInfoIndication`, post-rewrite gNB->AMF leg), `accessStratumRelease: spare1 (7)`. All sibling fields identical, isolating the attack to the one field — matches the concurrently-extracted `raw_12f.csv` row (`ue_category` 16->1). Correct frame selected on the first attempt. | Chapter 3 testbed-evidence section — Session B label-1 (Nothing 3A, Cat-downgrade) before/after N2 capability decode |
| `screenshots/wireshark_label3_nothing3a_beforeafter_20260810.png` | 2026-08-10T08:31:53Z | Side-by-side Wireshark: left = `capture_0_nothing3a_..._1.pcap` frame 42 (label-0 baseline) — `featureSetsDownlinkPerCC[0] -> maxNumberMIMO-LayersPDSCH: fourLayers`; right = `capture_3_nothing3a_nothing3a-3-run01-20260810T175015_1.pcap` frame 42 (post-rewrite, port 38413) — same `Item 0` branch, field entirely absent. Matches `raw_12f.csv` (`mimo_layers_dl` 4->1). **Finding**: this device's native uplink capability already declares `maxNumberMIMO-LayersCB-PUSCH: oneLayer` (SISO uplink natively, confirmed via direct `ngap_decode` diff before asking for the screenshot), so the attack's UL-forcing component is a genuine no-op here — only the DL field changes, unlike the Pixel 8/Realme pairs which showed both DL and UL moving. | Chapter 3 testbed-evidence section — Session B label-3 (Nothing 3A, MIMO-reduced) before/after N2 capability decode, DL-only-change finding |
| `screenshots/wireshark_label4_nothing3a_beforeafter_20260810.png` | 2026-08-10T08:41:10Z | Side-by-side Wireshark: left = `capture_0_nothing3a_..._1.pcap` frame 42 (label-0 baseline) — `nonCriticalExtension -> nonCriticalExtension -> ims-Parameters` present, between `overheatingInd` and the nested `nonCriticalExtension`; right = `capture_4_nothing3a_nothing3a-4-run01-20260810T175015_1.pcap` frame 43 (post-rewrite, port 38413) — `ims-Parameters` entirely absent, tree goes straight from `overheatingInd` to `nonCriticalExtension`. Matches `raw_12f.csv` (`vonr_supported` True->False); the Nothing 3A genuinely supports VoNR natively, so this is a real before/after pair (not a no-op like the Pixel 8's case). | Chapter 3 testbed-evidence section — Session B label-4 (Nothing 3A, VoNR-denied) before/after N2 capability decode |
| `screenshots/wireshark_label5_nothing3a_beforeafter_20260810.png` | 2026-08-10T08:49:51Z | Side-by-side Wireshark: left = `capture_0_nothing3a_..._1.pcap` frame 42 (label-0 baseline) — `rf-Parameters` has `supportedBandCombinationList`, `-v1540`, and `-v1610`, all present (1 item each); right = `capture_5_nothing3a_nothing3a-5-run01-20260810T175015_1.pcap` frame 37 (post-rewrite, port 38413) — only `-v1610` remains, base key and `-v1540` both stripped. **Real methodological finding**: `apply_ca_disabled()` in `ngap_decode.py` only clears `supportedBandCombinationList`/`-v1540`/`-v1560` — it has no knowledge of `-v1610`, a newer 3GPP release-extension variant this device happens to declare CA through. So the Combined (and CA-disabled, label 2) attack does not fully strip CA on the wire for this device; `ca_supported=False` in `raw_12f.csv` is still correct because `summarize_capability()` only reads the base key, but the true wire-level capability retains a residual CA declaration. Confirmed via direct `ngap_decode` diff before the screenshot, deliberately captured as-is (not "fixed" first) since it documents a real gap in the Stage 2 attack coverage, not a mistake. | Chapter 3 testbed-evidence section — Session B label-5 (Nothing 3A, Combined) before/after N2 capability decode; methodological finding — incomplete CA-stripping for `-v1610`-declaring devices |
| `screenshots/phone_nothing3a_speedtest_label0_20260810.png` | 2026-08-10T09:03:44Z | fast.com (Chrome, adb screencap) on the Nothing 3A under label 0 (no attack) — `31 Mbps` download, `32 Mbps` upload, `32ms`/`461ms` unloaded/loaded latency. | Chapter 3 testbed-evidence section — Session B before/after speedtest pair, label-0 half |
| `screenshots/phone_nothing3a_speedtest_label5_20260810.png` | 2026-08-10T09:06:23Z | fast.com (Chrome, adb screencap) on the Nothing 3A under label 5 (Combined attack, UE re-registered fresh first, confirmed via proxy log the attack applied - `ca_supported=False, mimo_dl_layers=None, vonr_supported=False`) — `23 Mbps` download, `29 Mbps` upload, `27ms`/`698ms` latency. **Contrast with the Pixel 8/Realme pairs**: here throughput genuinely dropped and loaded latency rose under the attack, matching naive expectation - the first device in this project to show that direction. Still not treated as proof the Pixel 8/Realme pairs were wrong (the single-cell-testbed CA-limitation caveat there still holds); more likely reflects normal test-to-test RF variance being large relative to any real capacity effect, now demonstrated pointing both ways across three independent handsets. | Chapter 3 testbed-evidence section — Session B before/after speedtest pair, label-5 half, third data point on the throughput-variance caveat |
| `screenshots/phone_nothing3a_vonr_setting_20260810.png` | 2026-08-10T09:09:07Z | Nothing 3A SIM settings screen ("srsRAN Test" carrier, adb screencap): a genuine `5G Calling` toggle (Nothing's naming for VoNR/Voice-over-5G) present, currently ON, alongside `4G Calling`, `Wi-Fi calling`, `Carrier video calling`. **Third distinct pattern in this project's VoNR-UI findings**: unlike the Pixel 8 (no capability, no UI toggle) and the Realme (capability present, no UI toggle - carrier-config-gated), this device shows both the RRC-layer capability (`voiceOverNR: supported`, confirmed in the label-4 exhibit above) and a matching, enabled UI toggle - the expected/consistent case. | Chapter 3 testbed-evidence section — Session B label-4 (Nothing 3A) VoNR capability-vs-UI consistency, third comparison point |
| `screenshots/phone_nothing3a_attached_5gsa_20260810.png` | 2026-08-10T09:13:54Z | G-NetTrack Lite on the Nothing 3A (adb screencap): `Operator: srsRAN Test`, `Type: 5G SA`, `Band: N78`, `RSRP: -63`. Retroactive Session A parity exhibit — this device was introduced directly into Session B and never went through a formal Session A pass like the other 5 profiles; this closes that gap alongside the InitialUEMessage-to-Registration-complete latency figure noted in the project's internal build log (~497ms avg, 6 samples - not necessarily the same narrower metric the original Session A table used for other profiles). History log below the live reading shows brief drops to a real commercial tower (cell 12153, 4G) during idle gaps between test events, the same known emergency-fallback behavior already documented for the A56's equivalent exhibit - not a new finding. | Chapter 3 testbed-evidence section — Session A parity, Nothing 3A real-handset attachment confirmation |
| `decoded_0.txt` | 2026-08-10T11:28:53Z | Full `tshark -V` dump of both proxy legs (frame at port 38412 pre-rewrite, frame at port 38413 post-rewrite/AMF-cached) for one representative label-0 registration, from `data/raw/exhibits/ngap_label0_full.pcap` | Chapter 4, W4.1 attack catalogue — reference exhibit for mode 0 (Normal), regenerable via `analysis/build_exhibits.py` |
| `decoded_1.txt` | 2026-08-10T11:28:53Z | Same, from `data/raw/validation/ngap_swstd_label1_full.pcap` — shows `accessStratumRelease: rel15` (pre-rewrite) vs `spare1` (post-rewrite/cached) | Chapter 4, W4.1 attack catalogue — reference exhibit for mode 1 (Cat-downgrade) |
| `decoded_2.txt` | 2026-08-10T11:28:53Z | Same, from `data/raw/validation/ngap_swstd_label2_full.pcap` — shows `supportedBandCombinationList` present (pre-rewrite) vs absent (post-rewrite/cached) | Chapter 4, W4.1 attack catalogue — reference exhibit for mode 2 (CA-disabled) |
| `decoded_3.txt` | 2026-08-10T11:28:54Z | Same, from `data/raw/validation/ngap_swstd_label3_full.pcap` — shows `maxNumberMIMO-LayersPDSCH`/`-LayersCB-PUSCH` reduced post-rewrite | Chapter 4, W4.1 attack catalogue — reference exhibit for mode 3 (MIMO-reduced) |
| `decoded_4.txt` | 2026-08-10T11:28:54Z | Same, from `data/raw/validation/ngap_swstd_label4_full.pcap` — shows `ims-Parameters` present (pre-rewrite) vs absent (post-rewrite/cached) | Chapter 4, W4.1 attack catalogue — reference exhibit for mode 4 (VoNR-denied) |
| `decoded_5.txt` | 2026-08-10T11:28:54Z | Same, from `data/raw/exhibits/ngap_label5_full.pcap` — shows all three of the above fields degraded together post-rewrite | Chapter 4, W4.1 attack catalogue — reference exhibit for mode 5 (Combined) |
| `decoded_6.txt` | 2026-08-10T11:28:54Z | Same, from `data/raw/exhibits/ngap_label6_full.pcap` — the 12 tracked N2 fields are unchanged between legs by design (target `supportedROHC-Profiles` is outside all 12 tracked features) | Chapter 4, W4.1 attack catalogue — reference exhibit for mode 6 (Partial/noise), documents the no-visible-field-change case |
| `xlayer_0.txt` through `xlayer_6.txt` | 2026-08-10T11:28:53Z – 11:28:54Z | Paired RRC-vs-N2 cross-layer decode for one Nothing 3A event per mode (`data/raw/{0..6}/capture_{mode}_nothing3a_..._10.pcap` + matching `data/raw/rrc/rrc_{mode}_nothing3a_..._10.json`): the RRC-side record, the N2-side (post-rewrite) record, `xlayer()` against the same-registration RRC echo (documented inline as circular for a case-b reattach), and `xlayer()` against the persisted case-a reference `data/raw/rrc/reference/001010000000003.json` (the project's actual non-circular methodology). Mode 2's exhibit shows `ca_supported_match: false, ca_band_count_delta: -1` as expected. | Chapter 4, W4.4 cross-layer divergence catalogue — one reference exhibit per mode, all 7 |

**Session C2 batch (2026-08-10, `session_id=session-c2-analysis-20260810`)**: the 14
`decoded_*.txt`/`xlayer_*.txt` rows above, plus the 4 W4.1-W4.4 result tables
(`../table_attack_catalogue.md`, `../table_xlayer_divergence.md`,
`../table_feature_significance.md`, `../table_intraclass_consistency.md`) and
`../fig_feature_distributions.png`, were all custody-appended this session (19
records) — the first time this project's `analysis/` build outputs themselves
(not just the raw pcaps/screenshots feeding them) have been custody-chained,
matching the convention already used for every other Chapter 3/4 exhibit here.
Full end-to-end chain re-verification (all 8,199 records recomputed from the
genesis hash forward) confirmed no break. All 5 build scripts that produce
these outputs (`../build_table1_attack_catalogue.py`,
`../build_table2_xlayer_divergence.py`, `../build_w42_feature_significance.py`,
`../build_w43_intraclass_consistency.py`, `../build_exhibits.py`) are committed
alongside their outputs, so every row above is regenerable from the preserved
PCAPs/CSVs rather than being a one-off, non-reproducible capture. Full
narrative in the project's internal build log's "CLI Session C2" section.
| `screenshots/phone_pixel8_vonr_setting_20260809.png` | 2026-08-09T00:25:58Z | Pixel 8 Pro SIM settings screen ("srsRAN Test"): `VoLTE` toggle present, but no `VoNR`/"Voice over 5G" entry anywhere in the list (Roaming, App data usage, VoLTE, Preferred network type, Calling section with Wi-Fi calling/Carrier video calling). Documents the *absence* of the toggle, not its state — consistent with the RRC-layer finding that every genuine Pixel 8 capture (imsi ...0002, all labels) shows `vonr_supported=False`, and with `amf.log`'s recurring `Ue requested DNN "ims" Not Supported` warning (no IMS APN provisioned on this test SIM). Label-4 (VoNR-denied) attack is a true no-op on this handset/SIM combination — see collection_manifest.csv notes; the label-4 Wireshark before/after checkpoint was skipped for the same reason, after 2 retries confirmed VoNR is never natively advertised. | Chapter 3 testbed-evidence section — Session B label-4 (Pixel 8) VoNR-absence finding, real-handset SIM provisioning caveat |
| `screenshots/wireshark_n2_flow_nothing3a_20260812.png` | 2026-08-12T00:00:37Z | Wireshark decode of `data/raw/baseline/baseline_nothing3a.pcap`, `ngap` filter, frame 54 (`UERadioCapabilityInfoIndication`, port 38413, post-rewrite/AMF-cached leg — the sibling pre-rewrite frame is 51 on port 38412) — `accessStratumRelease: rel16 (1)`, matching the documented Nothing 3A fingerprint. Retroactive Session A dual-leg baseline exhibit (post-Session-F, 2026-08-12), closing the gap left by Nothing 3A's original mid-Session-B introduction (see the project's internal build log's updated "CLI Session A" table and its retroactive-capture note). | Chapter 3 testbed-evidence section — Session A parity, Nothing 3A dual-leg (RRC-at-gNB + NGAP-at-N2) capability confirmation |

## Related evidence not indexed above (internal QA, not curated exhibits)

`data/raw/validation/ngap_swstd_label{1,2,3,4,5,6}_full.pcap` — the full labels 1-6
validation sweep against the SW-Std baseline, re-run 2026-07-30 after the label-1
("Cat downgrade") modifier fix (`ngap_decode.py`'s `apply_cat_downgrade` was
re-targeted from an untracked `pdcp-Parameters`/`featureSets` change to
`accessStratumRelease`, which `extract_features.py`'s `ue_category` feature
directly encodes — see the project's internal build log's "Build 0b fix" section for the full writeup,
including why `spare1` and not the architecture doc's literal "rel8" was used).
Each pcap captures both proxy legs (Bug 16) for one live UERANSIM registration
(two for label 6, to exercise both branches of the seeded RNG), and
`extract_features.extract()` confirms each label isolates exactly the field(s)
its modifier targets, with no cross-contamination between labels.

`data/raw/validation/ngap_swext_label0_full.pcap` and `ngap_swmin_label0_full.pcap`
— the SW-Ext and SW-Min Stage 1 profile-fingerprint captures (Build 0a exit-checklist
Step 4). These pre-date the label-1 fix and are unaffected by it (label 0 is a
pure Stage 1 pass-through, no Stage 2 modifier applied). `extract_features.extract()`
confirms SW-Ext's post-rewrite event as `ca_supported=True, ca_band_count=1,
mimo_layers_dl=4, mimo_layers_ul=2, vonr_supported=True, nr_band_count=4` (the
documented extended/high-end fingerprint) and SW-Min's as `ca_supported=False,
ca_band_count=0, mimo_layers_dl=1, mimo_layers_ul=1, vonr_supported=False,
nr_band_count=1` (matches the SW-Std pre-rewrite/native-minimal shape, as
documented — SW-Min's Stage 1 baseline is deliberately minimal).

This directory holds curated Chapter 3 exhibits only (`mode=exhibit`,
`session_id=build-0a-evidence`) — the evidence above is internal QA for the
proxy/codec build, not dissertation-facing exhibits, so none of it is added to
the table. It is still custody-chained:
- The six SW-Std label sweep pcaps: `chain_of_custody.log`,
  `session_id=build-0b-swstd-sweep-20260730`, `attack_mode` =
  `cat_downgrade` / `ca_disabled` / `mimo_reduced` / `vonr_denied` /
  `combined` / `partial_noise` respectively.
- The two profile-fingerprint pcaps: `chain_of_custody.log`,
  `session_id=build-0a-profile-fingerprint-sweep-20260730`, `attack_mode=normal`
  (label 0) for both.

The full 17-record chain (9 exhibit/meta records + these 8) was re-verified
end-to-end (every chain hash recomputed from the genesis hash forward) with no
breaks, at that point in the build sequence. The chain and this document both
kept growing afterward — the labels 2-6 screenshot rows and the Build 0d
RRC-vs-N2 spike rows above were added later in the same day (30 records total
once 0d closed, per the project's internal build log's Build 0d section), and a further 13 records
were added 2026-07-31 for the output-capture recovery (`logs/transcripts/`,
unrelated to this document, plus one more once this file itself was
custody-appended - see the project's internal build log's "Output-capture recovery" note). Current
total as of 2026-07-31: **44 records**, chain-verified end-to-end, no breaks.
